# spec.md

## 概要
Tategaki は、寄付金一覧と寄付品一覧のデータを Excel から取り込み、編集・印刷できる Blazor WebAssembly アプリケーションです。

## 目的
- 寄付金データを簡単に取り込んで確認・編集・印刷できるようにする
- 寄付品データを簡単に取り込んで印刷向けレイアウトに整えられるようにする
- 印刷用の設定を保存・再利用できるようにする

## 機能要件
### 1. 寄付金一覧
- Excel ファイルから寄付金データを取り込めること
- 氏名・かな・金額・ソートキーを編集できること
- 縦書き印刷プレビューを確認できること
- 印刷設定を保存・復元できること

### 2. 寄付品一覧
- Excel ファイルから寄付品データを取り込めること
- 氏名・品物・数量・単位・ソートキーを管理できること
- 縦書き印刷プレビューを確認できること
- 印刷設定を保存・復元できること

### 3. 共通要件
- Web アプリケーションとしてブラウザ上で利用できること
- 印刷時は A4 横向きのレイアウトに対応すること
- 画面上で設定を変更した内容を保持できること
- 手動で調整したページ境界を保存し、再読み込みや設定のインポート後も復元できること
- 設定の JSON エクスポート / インポートを行えること
- Excel 取り込みパネルと詳細設定パネルは、同時に開かないように切り替えできること
- 寄付金用の設定モデルは `DonationMoneyPrintSettings` という命名で統一し、実装・ドキュメント上でも一貫して扱うこと
- 寄付金画面のヘッダーには、最終読込したワークブック名・シート名・読込日時を表示できること
- 寄付品画面のヘッダーにも、最終読込したワークブック名・シート名・読込日時を表示できること
- 印刷画面の操作パネルは不要説明文を表示せず、コンパクトな操作中心レイアウトであること

## 制約事項
- 既定の Excel 形式に準拠したデータを入力する前提とする
- バックエンドサーバーは不要とする
- 主要な印刷レイアウトはクライアント側の CSS と Razor コンポーネントで処理する

## 設定のエクスポート / インポート詳細仕様
### 1. 保存対象
- 設定ファイルには、フォント、余白、幅、行間、ページ境界の復元に必要なページごとのエントリ数などを含める
- 1つの設定ファイルは、寄付金または寄付品のいずれかの印刷設定を表す

### 2. エクスポート動作
- 画面上の「設定をエクスポート」操作で、現在の設定内容を JSON 形式でファイルとして保存できる
- エクスポート時には、現在表示中のページ境界を表す各ページのエントリ数を設定データに反映する
- ファイル名は日時付きの形式とし、管理しやすいようにする

### 3. インポート動作
- 画面上の「設定をインポート」操作で、JSON ファイルを読み込んで設定を反映できる
- インポート時には、設定ファイル内の保存内容を読み込む
- ただし、現在画面に表示されているレイアウト状態が存在する場合は、その状態を優先してページ境界の復元に利用する
- そのため、古い設定ファイルに含まれるページ境界情報が、現在の表示中のエントリ状態で上書きされることはない

### 4. 復元優先順位
- 1番目: 現在の画面上のレイアウト状態
- 2番目: 設定ファイルに含まれるページごとのエントリ数
- 3番目: どちらもない場合は既定の再配置で扱う

### 5. 制約・注意事項
- 設定ファイルはブラウザのローカル保存用であり、他環境との自動同期は行わない
- 設定内容は現在の画面表示内容に依存するため、異なる Excel データを読み込んだ場合は復元結果が変わることがある
- ページごとのエントリ数は、現在のレイアウト計算に基づいて保存・復元されるため、表示幅や文字数の違いで再現結果が変わることがある

## 今後の展望
- 寄付品の編集画面を実装する
- 入力 Excel のフォーマット差異に対応する
- より柔軟な印刷設定とレイアウト調整を追加する
